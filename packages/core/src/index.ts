/**
 * Pure core boundary. Product logic begins in E01; this module deliberately
 * has no platform imports, filesystem access, global application state, or I/O.
 */
export const CORE_PACKAGE_ID = 'privacy-bridge-core';
